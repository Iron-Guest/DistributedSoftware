[mysqld]
# 字符集配置
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# 主库 server-id
server-id = 1
# 开启二进制日志
log-bin = mysql-bin
# 复制的数据库
binlog-do-db = shopping_platform
# 二进制日志格式
binlog_format = ROW
# 每 N 次事务写入后同步到磁盘
sync_binlog = 1